package io.github.kukpt.sl651.server;

import io.vertx.core.Vertx;
import io.vertx.ext.unit.TestContext;

public class ServerIdleTest {

  private Vertx vertx = Vertx.vertx();

  public void test(TestContext context){
//    HydrologicalServer server = HydrologicalServer.create(vertx, new HydrologicalServerOptions().setTimeoutOnConnect(20));
//    server.endpointHandler(endpoint -> {
//
//    });
//    Async async = context.async();
//    server.listen()
//      .onComplete(res -> {
//        vertx.createNetClient().connect(11883, "127.0.0.1")
//          .onSuccess(conn -> {
//            Buffer buf = Buffer.buffer(new byte[]{
//              0x7e, 0x7e
//
//            });
//            conn.write(buf);
//          });
//      });
  }
}
